<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:Vm ="clr-namespace:SmartFillMonitor.ViewModels"
                    xmlns:Views="clr-namespace:SmartFillMonitor.Views">
    <SolidColorBrush x:Key="AppBgDark" Color="Black"/>
    <LinearGradientBrush x:Key="AppBgPannel" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#263552" Offset="0"/>
        <GradientStop Color="#1C2942" Offset="1"/>
    </LinearGradientBrush>
    <!--显示正常状态颜色-->
    <SolidColorBrush x:Key="AppNormal" Color="Green"/>
    <!--出错警告状态颜色-->
    <SolidColorBrush x:Key="AppFault" Color="Red"/>
    <!--通用边框颜色-->
    <SolidColorBrush x:Key="AppBoardBrush" Color="#3C4B63"/>

    <!--导航专用颜色-->
    <SolidColorBrush x:Key="NavBackground" Color="#1E2A42"/>
    <SolidColorBrush x:Key="NavItemForeground" Color="#CFE6FF"/>
    <SolidColorBrush x:Key="NavItemHoverBg" Color="#283654"/>
    <SolidColorBrush x:Key="NavItemCheckedBg" Color="#324264"/>
    <DataTemplate DataType="{x:Type Vm:DashBoardViewModel}">
        <Views:DashBoardView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type Vm:DashQueryViewModel}">
        <Views:DashQueryView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type Vm:AlarmViewModel}">
        <Views:AlarmView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type Vm:LogsViewModel}">
        <Views:LogsView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type Vm:SettingViewModel}">
        <Views:SettingView/>
    </DataTemplate>
    <Style x:Key="NavItemStyle" TargetType="RadioButton">
        <Setter Property="Background" Value="{StaticResource NavBackground}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{StaticResource NavItemForeground}"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource NavItemHoverBg}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Background" Value="{StaticResource NavItemCheckedBg}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>

            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="CardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource AppBgDark}"/>
        <Setter Property="BorderBrush" Value="{StaticResource AppBoardBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="5"/>
        <Setter Property="Padding" Value="15"/>
    </Style>
    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="Black"/>
        <Setter Property="BorderBrush" Value="Black"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="Padding" Value="15"/>
    </Style>
    <Style TargetType="Button">
        <Setter Property="Background" Value="Blue"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.9"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="TabItem" x:Key="AppTabItemStyle">
        <Setter Property="Foreground" Value="LightGray"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="Margin" Value="4,0,4,0"/>
        <Setter Property="MinWidth" Value="64"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Grid>
                        <Border Name="Border" Background="Transparent" BorderBrush="Transparent" BorderThickness="0" CornerRadius="5,5,0,0" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" ContentSource="Header"/>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource AppBgPannel}"/>
                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AppBoardBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="#777777"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#2E2E2E"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="TabControl" x:Key="AppTabControlStyle">
        <Setter Property="Background" Value="{StaticResource AppBgPannel}"/>
        <Setter Property="BorderBrush" Value="{StaticResource AppBoardBrush}"/>
        <Setter Property="Padding" Value="6"/>
        <Setter Property="ItemContainerStyle" Value="{StaticResource AppTabItemStyle}"/>
        <Setter Property="Template" >
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid >
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Background="Transparent" Padding="6,6,6,0">
                            <TabPanel x:Name="HeaderPanel" IsItemsHost="True" Margin="0"/>
                        </Border>
                        <Border Grid.Row="1" Background="{StaticResource AppBgPannel}" BorderBrush="{StaticResource AppBoardBrush}" BorderThickness="1" CornerRadius="0,5,5,5" Margin="0,8,0,0" Padding="{TemplateBinding Padding}">
                            <ContentPresenter Name="PART_SelectedContentHost" Margin="8" ContentSource="SelectedContent"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="Button" x:Key="OutLine">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource AppBoardBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="2">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Margin}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="Gray"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="Button" x:Key="AppButtonStyle">
        <Setter Property="Background" Value="Blue"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="2">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Margin}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.9"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>