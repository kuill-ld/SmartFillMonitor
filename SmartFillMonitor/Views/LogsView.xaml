<UserControl x:Class="SmartFillMonitor.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartFillMonitor.Views"
             mc:Ignorable="d" 
             xmlns:handyControl="clr-namespace:HandyControl.Controls;assembly=HandyControl"
             xmlns:smartFillMonitor="clr-namespace:SmartFillMonitor"
             d:DesignHeight="450" d:DesignWidth="1200">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDefault.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <TabControl Style="{StaticResource AppTabControlStyle}">
        <TabItem Header="Log" >
            <ContentControl Content="{x:Static smartFillMonitor:App.LogView}" />
        </TabItem>
        <TabItem Header="LogAnalyze">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions> 
                <Border Grid.Row="0" Background="{StaticResource AppBgDark}" CornerRadius="5" Margin="0,0,0,10">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="从:" Foreground="LightGray" VerticalAlignment="Center" FontSize="16" Margin="10"/>
                        <handyControl:DateTimePicker SelectedDateTime="{Binding StartDate}" Width="160" VerticalAlignment="Center" />
                        <TextBlock Text="到:" Foreground="LightGray" VerticalAlignment="Center" FontSize="16" Margin="10"/>
                        <handyControl:DateTimePicker SelectedDateTime="{Binding EndDate}" Width="160" VerticalAlignment="Center" />
                        <TextBlock Text="等级" Foreground="LightGray" VerticalAlignment="Center" FontSize="16" Margin="10"/>
                        <ComboBox SelectedItem="{Binding SelectedLevel,UpdateSourceTrigger=PropertyChanged}" ItemsSource="{Binding LogLevels}" Width="100" VerticalAlignment="Center"/>
                        <TextBlock Text="搜索:" Foreground="LightGray" VerticalAlignment="Center" FontSize="16" Margin="10"/>
                        <TextBox Text="{Binding SearchText ,UpdateSourceTrigger=PropertyChanged}" Width="200" VerticalAlignment="Center"  Foreground="LightGray" ToolTip="输入消息内容来源进行搜索" FontSize="16" Margin="10">
                            <TextBox.InputBindings>
                                <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                            </TextBox.InputBindings>
                        </TextBox>
                        <Button Content="查询" Command="{Binding SearchCommand}" Width="80" Margin="10" Background="Blue" Foreground="LightGray"/>
                        <Button Content="导出" Command="{Binding ExportCommand}" Width="80" Margin="10" Background="Green" Foreground="LightGray"/>
                    </StackPanel>
                </Border>
                <DataGrid Grid.Row="1" ItemsSource="{Binding Logs}" AutoGenerateColumns="True" IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal" BorderThickness="0"/>
                <Grid Grid.Row="2" Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Visibility="{Binding IsBusy, Converter={StaticResource Boolean2VisibilityConverter}}">
                        <TextBlock Text="正在查询:" Foreground="Green"  VerticalAlignment="Center" FontSize="16" Margin="10"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <TextBlock Text="{Binding TotalCount, StringFormat='共 {0} 条记录'}" Foreground="LightGray" VerticalAlignment="Center" FontSize="16" Margin="10"/>
                        <Button Content="上一页" Command="{Binding PrePageCommand}" Foreground="White" Width="80" Background="{StaticResource AppBgDark}"/>
                        <TextBlock Text="{Binding PageIndex, StringFormat='第{0}页 '}" Foreground="LightGray" VerticalAlignment="Center" FontSize="16" Margin="10"/>
                        <Button Content="下一页" Command="{Binding NextPageCommand}" Foreground="White" Width="80" Background="{StaticResource AppBgDark}"/>
                    
                    </StackPanel>
                </Grid>
            </Grid>
        </TabItem>
    </TabControl>

</UserControl>
