<UserControl x:Class="SmartFillMonitor.Views.DashBoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartFillMonitor.Views"
             mc:Ignorable="d" 
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             d:DesignHeight="800" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,20,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <!--产量-->
            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,10,0">
                <StackPanel >
                    <TextBlock Text="当前产量(PCS)" Foreground="Brown" Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding ActualCount,StringFormat='{}{0:N0}'}" Foreground="Brown" FontSize="32" FontWeight="Bold"/>
                    <TextBlock  Foreground="Brown" FontSize="12" Margin="0,5,0,0">
            <Run Text="目标达成率"/>
            <Run Text="{Binding ActualCount}"/>
            <Run Text="/"/>
            <Run Text="{Binding TargetCount}"/>
                    </TextBlock>
                </StackPanel>
            </Border>
            <!--温度-->
            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="0,0,10,0">
                <StackPanel >
                    <TextBlock Text="实时温度" Foreground="Brown" Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding CurrentTemp,StringFormat='{}{0:N0}'}" Foreground="Brown" FontSize="32" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="设定值" Foreground="Brown" FontSize="12" Margin="0,5,0,0"/>
                        <TextBlock Text="{Binding SettingTemp}" Foreground="Brown" FontSize="12" Margin="0,5,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Border>
            <!--运行时间-->
            <Border Grid.Column="2" Style="{StaticResource CardStyle}" Margin="0,0,10,0">
                <StackPanel >
                    <TextBlock Text="运行时间(H)" Foreground="Brown" Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding RunningTime,StringFormat='{}{0:N0}'}" Foreground="Brown" FontSize="32" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="设备状态" Foreground="Brown" FontSize="12" Margin="0,5,0,0"/>
                        <TextBlock Text="{Binding DeviceStatus}" Foreground="{StaticResource AppNormal}" FontSize="12" Margin="0,5,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Border>
            <!--节拍-->
            <Border Grid.Column="3" Style="{StaticResource CardStyle}" Margin="0,0,10,0">
                <StackPanel >
                    <TextBlock Text="当前节拍" Foreground="Brown" Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding CurrentCycleTime,StringFormat='{}{0:N0}'}" Foreground="Brown" FontSize="32" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="标准(s)" Foreground="Brown" FontSize="12" Margin="0,5,0,0"/>
                        <TextBlock Text="{Binding StandardCycleTime}" Foreground="Brown" FontSize="12" Margin="0,5,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="300"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!--罐装工位实时状态-->
            <Border Grid.Column="0" Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,10,10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock Text="罐装工位实时状态" FontSize="16" FontWeight="Bold" HorizontalAlignment="Left" Foreground="Brown"/>
                        <ContentControl HorizontalAlignment="Right" >
                            <ContentControl.Template>
                                <ControlTemplate TargetType="ContentControl">
                                    <StackPanel x:Name="Container" Orientation="Horizontal">
                                        <Ellipse x:Name="valueEllipse" Width="10" Height="10" Fill="Gray"  Margin="0,0,5,0"/>
                                        <TextBlock x:Name="ValueText" VerticalAlignment="Center" Text="阀门关闭" Foreground="Gray" FontSize="12"/>
                                    </StackPanel>
                                    <ControlTemplate.Triggers>
                                        <DataTrigger Binding="{Binding ValueOpen}" Value="true">
                                            <Setter TargetName="valueEllipse" Property="Fill" Value="{StaticResource AppNormal}"/>
                                            <Setter TargetName="ValueText" Property="Foreground" Value="{StaticResource AppNormal}"/>
                                            <Setter TargetName="ValueText" Property="Text" Value="阀门打开"/>
                                            <DataTrigger.EnterActions>
                                                <BeginStoryboard >
                                                    <Storyboard RepeatBehavior="Forever" AutoReverse="true">
                                                        <DoubleAnimation Storyboard.TargetName="valueEllipse" Storyboard.TargetProperty="Opacity" From="1" To="0.35"   Duration="0:0:1"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.EnterActions>
                                        </DataTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ContentControl.Template>
                        </ContentControl>
                    </Grid>
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Row="0" HorizontalAlignment="Center">
                            <Border Background="#333" Height="200" Width="150" BorderThickness="2" BorderBrush="#555">
                                <Grid>
                                    <Rectangle x:Name="LiquidLevel" VerticalAlignment="Bottom" Opacity="0.8" Height="{Binding LiquidLevel}">
                                        <Rectangle.Fill>
                                            <LinearGradientBrush StartPoint="0,1" EndPoint="0,0">
                                                <GradientStop Color="#007Acc" Offset="0"/>
                                                <GradientStop Color="#00AAFF" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Rectangle.Fill>
                                    </Rectangle>
                                    <TextBlock Text="原料桶" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                            <TextBlock  HorizontalAlignment="Center" Margin="0,10,0,0">
                                <Run Text="液位:" Foreground="Gray"/>
                                <Run Text="{Binding LiquidLevel}" Foreground="White"/>
                                <Run Text="%" Foreground="Gray"/>
                            </TextBlock>
                        </StackPanel>
                        <Border Grid.Column="1" BorderBrush="#444" BorderThickness="1">
                            <Grid>
                                <Rectangle Height="10" VerticalAlignment="Bottom" Margin="0,0,0,20" Fill="#555"/>
                                <Border Width="40" Height="80" VerticalAlignment="Bottom" HorizontalAlignment="Center" Background="Blue" Margin="0,0,0,30">
                                    <TextBlock Text="Bottle" Foreground="White" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <!--阀门标签-->
                                <TextBlock Text="罐装阀门" Foreground="Gray" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0"/>
                                <Rectangle Width="4" Height="50" Fill="Cyan" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,50,0,0" Visibility="{Binding ValueOpen,Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </Border>
            <Border Grid.Column="1" Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="最新报警" FontSize="16" FontWeight="Bold" Foreground="Brown"/>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding RecentAlarms}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#333" BorderThickness="0,0,0,1" Padding="5" >
                                        <TextBlock FontSize="12">
                                            <Run Text="[" Foreground="White"/>
                                            <Run Text="{Binding TimeStr}" Foreground="White"/>
                                            <Run Text="]" Foreground="White"/>
                                            <Run Text="{Binding Title}" Foreground="White"/>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
            <Border Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="50"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="温度趋势曲线(最近1小时)" Foreground="Brown" FontSize="14" Margin="0,0,0,15"/>
                    <Border Grid.Row="1" Name="ChartContainer" Background="#111" BorderBrush="#333" BorderThickness="1" MinHeight="150">
                        <lvc:CartesianChart Series="{Binding TempLiveCharts}" LegendLocation="None" Background="Transparent" DisableAnimations="False" Margin="8">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis IsEnabled="true">
                                    <lvc:Axis.Separator>
                                        <lvc:Separator IsEnabled="False"/>
                                    </lvc:Axis.Separator>
                                </lvc:Axis>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis MinValue="0" MaxValue="150" ShowLabels="True">
                                    <lvc:Axis.Separator >
                                        <lvc:Separator IsEnabled="False"/>
                                    </lvc:Axis.Separator>
                                </lvc:Axis>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </Border>
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                        <Button Content="启动生产" Command="{Binding StartProductionCommand}" Margin="0,0,10,0" Width="120"/>
                        <Button Content="停止" Command="{Binding StopProductionCommand}" Background="Orange"  Margin="0,0,10,0" Width="120"/>
                        <Button Content="复位" Command="{Binding ResetProductionCommand}" Style="{StaticResource OutLine}" Margin="0,0,10,0" Width="120"/>
                       
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
