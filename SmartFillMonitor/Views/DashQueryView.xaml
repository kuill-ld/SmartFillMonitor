<UserControl x:Class="SmartFillMonitor.Views.DashQueryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartFillMonitor.Views"
              xmlns:handyControl="clr-namespace:HandyControl.Controls;assembly=HandyControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="1200">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDefault.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Background="{StaticResource AppBgDark}" CornerRadius="5" Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="开始:" Foreground="LightGray" VerticalAlignment="Center" FontSize="16" Margin="10"/>
                    <handyControl:DateTimePicker SelectedDateTime="{Binding StartDate}" Width="160" VerticalAlignment="Center" />
                    <TextBlock Text="结束" Foreground="LightGray" VerticalAlignment="Center" FontSize="16" Margin="10"/>
                    <handyControl:DateTimePicker SelectedDateTime="{Binding EndDate}" Width="160" VerticalAlignment="Center" />
                    <Button Content="查询" Command="{Binding QueryCommand}" Width="80" Margin="10" Background="Blue" Foreground="LightGray"/>
                    <Button Content="导出" Command="{Binding ExportCommand}" Width="80" Margin="10" Background="Green" Foreground="LightGray"/>
                </StackPanel>
            </Border>
            <DataGrid Grid.Row="1" ItemsSource="{Binding Records}" SelectedItem="{Binding SelectedRecord}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal" BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="时间" Binding="{Binding Time, StringFormat=yyyy-MM-dd HH:mm:ss}" Width="200"/>
                    <DataGridTextColumn Header="批次号" Binding="{Binding BatchNo}" />
                    <DataGridTextColumn Header="实产" Binding="{Binding ActualCount}"/>
                    <DataGridTextColumn Header="目标" Binding="{Binding TargetCount}" />
                    <DataGridTextColumn Header="设定温度" Binding="{Binding SettingTemp}" />
                    <DataGridTextColumn Header="是否NG" Binding="{Binding isNG}" />
                </DataGrid.Columns>
            </DataGrid>

        </Grid>
    </Grid>
</UserControl>
