<UserControl x:Class="SmartFillMonitor.Views.AlarmView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartFillMonitor.Views"
             mc:Ignorable="d" 
             xmlns:hc ="clr-namespace:HandyControl.Controls;assembly=HandyControl"
             xmlns:converters="clr-namespace:SmartFillMonitor.Converters"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDefault.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <converters:NumToVisibilityConverter x:Key="NumToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>
    <TabControl Style="{StaticResource AppTabControlStyle}">
        <TabItem Header="活动报警">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="12" >
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="当前活动报警" FontSize="20" Foreground="White" />
                        <Border Margin="10,0" Background="Red" CornerRadius="10" Padding="8,4" VerticalAlignment="Center">
                            <TextBlock Text="{Binding ActiveAlarmCount}" Foreground="White" FontWeight="Bold" FontSize="16"/>
                        </Border>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" >
                        <Button Command="{Binding RefreshCommand}" HorizontalAlignment="Right" Width="90" Style="{StaticResource OutLine}">
                            <TextBlock>
                                    <Run Text="&#xe6b2;" FontFamily="{StaticResource IconFont}"/>
                                    <Run Text="刷新"/>
                            </TextBlock>
                        </Button>
                    </StackPanel>
                </Grid>
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding ActiveAlarms}" Margin="10" >
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="5" Background="#2d1111" BorderBrush="{StaticResource AppBoardBrush}" BorderThickness="4,0,0,0" Margin="0,0,0,10">
                                    <Grid >
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                            <TextBlock FontSize="15">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="#FFCCCC"/>
                                                    </Style>
                                                </TextBlock.Style>
                                            <Run Text="{Binding Code}" />
                                            <Run Text="{Binding Title}"/>

                                            </TextBlock>
                                            <TextBlock Text="{Binding Description}" Margin="0,5,0,5" Foreground="#AAAAAA"  FontSize="14"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="20,0,0,0">
                                            <TextBlock Text="{Binding TimeStr}" Foreground="#888888" FontSize="12" HorizontalAlignment="Right"/>
                                            <Button Content="确认/复位" Style="{StaticResource AppButtonStyle}" Command="{Binding DataContext.AckConfirmCommand ,RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Margin="0,5,0,0" Background="#555555" Width="90"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
                <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding ActiveAlarmCount,Converter={StaticResource NumToVisibilityConverter}}">
                    <TextBlock Text="√" FontSize="48" Foreground="{StaticResource AppNormal}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock Text="当前没有活动报警" FontSize="30" Foreground="Gray" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </TabItem>
        <TabItem Header="历史报警">
            <Grid Background="{StaticResource AppBgPannel}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <Border Grid.Row="0" Background="{StaticResource AppBgPannel}" BorderBrush="{StaticResource AppBoardBrush}" CornerRadius="4" BorderThickness="1" Margin="12,12,12,8" Padding="12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition  Width="auto"/>
                            <ColumnDefinition  Width="auto"/>
                            <ColumnDefinition  Width="auto"/>
                            <ColumnDefinition  Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="开始时间" FontSize="10" Foreground="White" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <hc:DateTimePicker SelectedDateTime="{Binding HistoryStartDate, UpdateSourceTrigger=PropertyChanged}" FontSize="12" Padding="4"/>
                        </StackPanel>
                        <StackPanel Margin="8,0,0,0" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="至" FontSize="10" Foreground="White" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <hc:DateTimePicker SelectedDateTime="{Binding HistoryEndDate, UpdateSourceTrigger=PropertyChanged}" FontSize="12" Padding="4"/>
                        </StackPanel>
                        <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                            <Button Content="查询" Command="{Binding LoadHistoryCommand}" Style="{StaticResource OutLine}" BorderBrush="Green" Width="80" Margin="10,0,8,0"/>
                        </StackPanel>
                        
                    </Grid>
                </Border>
                <Border Grid.Row="1" Background="{StaticResource AppBgPannel}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Margin="12,0,12,12">
                    <DataGrid ItemsSource="{Binding HistroyAlarms}" IsReadOnly="True" AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Code" Binding="{Binding Code}"/>
                            <DataGridTextColumn Header="Title" Binding="{Binding Title}"/>
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}"/>
                            <DataGridTextColumn Header="TriggerTime" Binding="{Binding TimeStr}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
                
            </Grid>
        </TabItem>
        <TabItem Header="测试报警">
            <Button Content="触发测试报警" Command="{Binding TestAlarmCommand}" Width="150" Height="40" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </TabItem>
    </TabControl>
</UserControl>
